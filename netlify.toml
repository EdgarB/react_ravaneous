[build]
  functions = "functions/"
  publish = "src"
[development]
  functions = "functions/"
  publish = "src"
[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"